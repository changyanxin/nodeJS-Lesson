流的概念：
在一个应用程序中，流是一组有序地，有起点和终点的字节数据的传输手段

流的工作过程：
在应用程序中各种对象之间交换与传输数据的时候，总是将该对象中的各种数据转化为流数据，通过留的传输，到达目的对象之后再将流数据转化为目的对象中所需数据

流的继承：
在Node.JS中，使用各种实现了stream.Readable接口的对象读取资源对象的数据称为流数据，这些对象都继承了EventEmitter类的实例对象，流具有事件的能力，可以监听流达到目的